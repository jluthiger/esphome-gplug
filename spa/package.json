{
  "name": "gplug-spa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "gPlug setup wizard SPA (Preact + htm), bundled into a single gzipped index.html for the ESPHome firmware",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node mock/server.mjs",
    "presets": "python3 tools/scripts2presets.py"
  },
  "dependencies": {
    "htm": "^3.1.1",
    "preact": "^10.26.9"
  },
  "devDependencies": {
    "esbuild": "^0.24.2"
  }
}
